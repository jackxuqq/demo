package main

import "fmt"

func help(a, b []int, k int) []int {
	ret := []int{}

	//get all zuhe
	for i := 0; i < len(a); i++ {
		for j := 0; j < len(b); j++ {

		}
	}

	return ret
}

func main() {

	a := []int{1, 2, 3}
	b := []int{4, 5, 6}
	k := 6
	fmt.Println(help(a, b, k))
}
